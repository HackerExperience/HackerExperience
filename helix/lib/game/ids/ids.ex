defmodule Game.Connection.ID, do: use(Core.ID.Definition)
defmodule Game.ConnectionGroup.ID, do: use(Core.ID.Definition)
defmodule Game.File.ID, do: use(Core.ID.Definition)
defmodule Game.Installation.ID, do: use(Core.ID.Definition)
defmodule Game.Log.ID, do: use(Core.ID.Definition)
defmodule Game.LogRevision.ID, do: use(Core.ID.Definition)
defmodule Game.Process.ID, do: use(Core.ID.Definition)
defmodule Game.Server.ID, do: use(Core.ID.Definition)
defmodule Game.ScannerInstance.ID, do: use(Core.ID.Definition)
defmodule Game.Tunnel.ID, do: use(Core.ID.Definition)
