--------------------------------------------------------------------------------
----------------------------------- INSERTS ------------------------------------
--------------------------------------------------------------------------------

-- :__insert
INSERT INTO installations (
  file_type, file_version, file_id, memory_usage, inserted_at
  )
VALUES
  (?, ?, ?, ?, ?)
RETURNING *;

--------------------------------------------------------------------------------
----------------------------------- SELECTS ------------------------------------
--------------------------------------------------------------------------------

-- :by_file_type_and_version
SELECT *
FROM installations
WHERE file_type = ? AND file_version = ?;
